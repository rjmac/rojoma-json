package com.rojoma

/** This package and its sub-packages contain things which for
  * various reasons need to be public but which should never be
  * explicitly used by user code.  Binary compatibility guarantees
  * hold only so long as this is true. */
package object `json-impl`
